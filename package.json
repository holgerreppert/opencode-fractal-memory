{
  "name": "opencode-fractal-memory",
  "version": "0.2.0",
  "description": "Fractal memory system for OpenCode with semantic search and automatic compression.",
  "main": "dist/plugin.js",
  "type": "module",
  "files": [
    "dist",
    "agent",
    "commands",
    "management",
    "scripts"
  ],
  "keywords": [
    "opencode",
    "opencode-plugin",
    "memory",
    "embeddings",
    "semantic-search",
    "hnsw",
    "vector-search",
    "bm25",
    "agent-memory"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/holgerreppert/opencode-fractal-memory"
  },
  "trustedDependencies": [],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build && npm run typecheck",
    "postinstall": "mkdir -p ~/.config/opencode/agent ~/.config/opencode/commands && cp -f agent/*.md ~/.config/opencode/agent/ && cp -f commands/*.md ~/.config/opencode/commands/ && bun run scripts/download-models.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "view": "bun run dist/management-standalone.js",
    "mcp": "bun run dist/mcp-server.js"
  },
  "dependencies": {
    "@huggingface/tokenizers": "^0.1.3",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "hnsw": "^1.1.1",
    "js-yaml": "^4.2.0",
    "onnxruntime-web": "^1.26.0",
    "zod": "^4.4.3"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.15.13"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.15.13",
    "@tsconfig/bun": "^1.0.10",
    "@types/bun": "^1.3.14",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "overrides": {}
}
