---
description: Get session tool call statistics
---
Show me session statistics including tool call timeline, files touched, and memory correlation. Use context(mode="session_stats") with a session_id to see what happened in a specific session.
